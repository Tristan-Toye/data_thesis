# Arithmetic Intensity

**File:** `arithmetic_intensity.png`

Bar chart of estimated arithmetic intensity (operations per byte of memory traffic) for each node. Nodes below the ridge point (~0.5--2.0 FLOPs/byte depending on precision) are in the memory-bound regime of the roofline model.
