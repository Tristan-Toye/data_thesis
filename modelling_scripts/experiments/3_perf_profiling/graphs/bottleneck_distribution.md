# Bottleneck Distribution

**File:** `bottleneck_distribution.png`

Pie or bar chart showing how many nodes fall into each bottleneck class (memory, compute, cache, branches). In this dataset, 8 of 15 nodes are memory-bound, confirming that memory access optimisation is the highest-leverage improvement area.
