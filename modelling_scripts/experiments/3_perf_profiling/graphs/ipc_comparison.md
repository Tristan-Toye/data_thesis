# IPC Comparison

**File:** `ipc_comparison.png`

Bar chart comparing Instructions Per Cycle (IPC) across all profiled nodes. Higher IPC indicates more efficient use of the CPU pipeline. Nodes with very low IPC (< 1.0) are likely stalled on memory accesses or branch mispredictions.
