# Memory Intensity vs IPC

**File:** `memory_intensity_vs_ipc.png`

Scatter plot with memory intensity (LLC misses per instruction) on the x-axis and IPC on the y-axis. Nodes in the bottom-right quadrant are memory-bound (high memory intensity, low IPC). Nodes in the top-left are compute-efficient.
