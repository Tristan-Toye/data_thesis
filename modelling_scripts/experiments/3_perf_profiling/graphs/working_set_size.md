# Working Set Size

**File:** `working_set_size.png`

Bar chart showing the estimated data working set (KB) for each node, derived from TLB miss behaviour. Nodes with working sets larger than the LLC benefit most from data layout optimisation or streaming/prefetch strategies.
