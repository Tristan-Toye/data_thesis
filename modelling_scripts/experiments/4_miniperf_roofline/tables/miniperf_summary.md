# Miniperf Combined Summary

**File:** `miniperf_summary.csv`

Merged view combining roofline aggregate data with stat counters. Each row is one node with roofline metrics (AI, performance, bound) alongside hardware counters (cycles, instructions, IPC).

This is the primary input for the cross-methodology comparison in experiment 5.
