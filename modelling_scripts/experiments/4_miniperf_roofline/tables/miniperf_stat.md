# Miniperf Stat Counters

**File:** `miniperf_stat.csv`

Per-node PMU counter snapshots collected via `mperf stat` (averaged across repetitions). Provides cycles, instructions, IPC, and other basic counters as a sanity check against experiment 3's perf data.
